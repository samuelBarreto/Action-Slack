name: 'Test Action (No Cache)'

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout code'
        uses: actions/checkout@v4
      
      - name: 'Test Action (No Cache)'
        uses: ./
        with:
          webhook-url: ${{ secrets.SLACK_WEBHOOK_URL }}
          message: '🧪 Teste da Action sem cache - Funcionando!'
          channel: '#testes'
          username: 'No Cache Bot'
          icon-emoji: ':white_check_mark:' 